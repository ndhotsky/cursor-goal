# Get this from Cursor's integrations/API dashboard.
CURSOR_API_KEY=crsr_...

# Optional. The CLI defaults to Composer 2.5 and resolves it through Cursor.models.list().
CURSOR_GOAL_MODEL=composer-2.5
